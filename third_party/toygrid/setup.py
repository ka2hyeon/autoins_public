import glob
from setuptools import setup
from setuptools import find_packages

setup(name='toygrid',
      version='1.0',
      description='toygrid env',
      author='ka2hyeon',
      author_email='',
      url='',
      packages=find_packages()
)
